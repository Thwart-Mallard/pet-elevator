# /etc/default/elevator-camera
# Copied to each Pi Zero 2 W and edited before enabling the service.

# Which floor this camera node watches (0 = ground, 1 = upper)
ELEVATOR_FLOOR=0

# Hostname or IP of the Pi 4 running the Mosquitto broker
ELEVATOR_BROKER=pet-elevator.local
